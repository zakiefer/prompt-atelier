---
id: attachment-you-are-firing-v89-3-hf-hotfix-on-v89-3-on-the-kapital-next-repo-v89-9a5a3d64
title: "You are firing V89-3-HF (hotfix on V89-3) on the kapital-next repo. V89..."
source: attachment
createdAt: 2026-07-03T00:00:00.000Z
---

You are firing V89-3-HF (hotfix on V89-3) on the kapital-next repo. V89-3 added a duplicate top chrome inside the dashboard component (.dashboard-topbar with its own workspace selector, breadcrumb, search, notification pill, avatar, sign-out) while AdminWorkspaceHeader from src/app/admin/layout.tsx already provides all of that. Both render. Three real bugs: (1) duplicate top chrome, (2) zero dark mode coverage for any V89-3 dashboard classes, (3) the duplicate notification pill is non-functional (not wired to NotificationCenter). Plus the AI rail layout squeezes the KPI strip on standard viewports. Branch-only. Do NOT fire merge from this chat. Use a fresh sibling clone. Standing rules in AGENTS.md apply. Strategic-debt budgets must hold: src/lib/notifications.ts 1473/1490, prisma/schema (combined) 5938/5938. All existing CSS contract guards must continue passing: check:ui-class-definitions (V88-4), check:z-index-scale (V88-6), check:hf-2-cold-load-locality (HF-2/3), check:v85-task-e (V85 E), check:v89-1-typography (V89-1), check:v89-2-token-spine (V89-2), check:v89-3-dashboard (V89-3). Inputs: Base origin/main 54b63ecb6deccc618bb15f0f54ad06215c9bbb5a. Closes V89-3-HF. Expected post-branch manifest UNCHANGED at 75/62 (no new guard — this is a hotfix on existing V89-3 work). V89-3-HF scope (do exactly these): (1) REMOVE the entire .dashboard-topbar block from src/components/admin/admin-dashboard-screen.tsx (lines 684-716 approximately — the wrapping `<div className="dashboard-topbar">` containing workspace selector + breadcrumb + search + notification pill + avatar link + SignOutButton). Also remove unused imports that become dead after removal (Search, Bell from lucide-react likely become unused — verify and remove). The AdminWorkspaceHeader from layout.tsx is the canonical admin header — V89-3-HF restores it as the single source of truth for top chrome on the admin dashboard. (2) REMOVE corresponding CSS in src/app/admin/admin.css: all rules for .dashboard-topbar, .dashboard-workspace-selector, .dashboard-breadcrumb, .dashboard-command-search, .admin-dashboard-notification-pill, .dashboard-avatar-link. The .dashboard-scope-filter-row (All books / All properties / Apply) is legitimate dashboard-page scope filtering — KEEP IT and its CSS. (3) UPDATE tests/v89-3-dashboard.test.ts:205 assertion: replace `expect(container.querySelector(".dashboard-topbar")).toBeTruthy();` with `expect(container.querySelector(".dashboard-topbar")).toBeFalsy();` to ASSERT REMOVAL (prevents future regression where someone re-adds duplicate top chrome). (4) ADD comprehensive dark mode coverage in admin.css. For EVERY V89-3 dashboard class — .v89-dashboard, .dashboard-shell, .dashboard-main, .dashboard-page-header, .dashboard-eyebrow, .dashboard-date-range, .dashboard-date-range button, .dashboard-date-range button[aria-pressed="true"], .kpi-strip, .kpi-tile, .kpi-label, .kpi-metric, .kpi-meta, .kpi-delta-positive, .kpi-delta-negative, .kpi-spark, .dashboard-mid-body, .dashboard-chart, .dashboard-activity, .dashboard-actions, .activity-row, .action-row, .dashboard-properties-table, .ai-rail, .ai-rail-header, .ai-rail-content, .ai-rail-suggestion — add scoped rules under `.app-theme-scope[data-app-theme="dark"] <selector>` that consume the V89-2 dark-mode token values (--color-surface-base, --color-surface-raised, --color-surface-overlay, --color-border-subtle, --color-border-strong, --color-ink-primary, --color-ink-secondary, --color-ink-tertiary). The tokens already have dark variants from V89-2; the V89-3 components just need to reference them at the appropriate selectors. Important: do NOT change light-mode appearance. Only add dark-mode overrides. (5) FIX AI rail layout. Current symptom: KPI strip overflows on viewports where AI rail is present. The .dashboard-shell should use grid-template-columns: 1fr 320px so the AI rail has a fixed 320px column and the .dashboard-main fills the remaining space. Verify the .kpi-strip inside .dashboard-main uses display: grid grid-template-columns: repeat(5, 1fr) gap: 32px so KPI tiles share the available width equally without overflowing into the rail. The "Decision 5" funnel pill should not get clipped — if active applications has stacked funnel pills that exceed the tile height, allow vertical overflow within the tile or use a horizontal scroll. (6) VERIFY the existing AdminWorkspaceHeader works on /admin after .dashboard-topbar removal. Specifically the Cmd-K search input should be visible in the workspace header (it already is — confirm), the notification center bell should be clickable and open the notification popup (it already does — confirm), and the avatar + sign-out should render (they already do — confirm). No new component work needed; we're just removing a duplicate, not building a new top bar. (7) Update docs/handoffs/v89-3-dashboard-audit.md (already in the v89-3-dashboard branch — find it and update) to note the .dashboard-topbar removal, dark mode addition, and AI rail layout fix. Do NOT add a new audit doc; extend the existing V89-3 audit. (8) Manifest: counts stay at 75/62 (no new guard — V89-3-HF is a fix on V89-3). Add a manifest row noting "V89-3-HF: duplicate top chrome removed, dark mode coverage added, AI rail layout fixed" but counts unchanged. Verification ladder per AGENTS.md: npm ci; npm run architecture:proof (budgets hold); npm run local:smoke; npm run check:v89-3-dashboard (V89-3 guard MUST still pass — the guard checks for kpi-strip, dashboard-chart, dashboard-activity, dashboard-actions, dashboard-properties-table, ai-rail classes which we're preserving); npm run check:v89-2-token-spine; npm run check:v89-1-typography; npm run check:ui-class-definitions; npm run check:z-index-scale; npm run check:hf-2-cold-load-locality; npm run check:v85-task-e; npx vitest run tests/v89-3-dashboard.test.ts (test now asserts .dashboard-topbar is ABSENT — PASS); npx vitest run --maxWorkers=8; npm run build (158/158). COLD-LOAD VERIFICATION — Playwright channel:"chrome", headless:false, fresh persistent profile per URL: PORT=3086 npm run dev. Cold-load with NO prior navigation: (a) http://localhost:3086/admin LIGHT MODE desktop 1440x900 — CDP verify: zero .dashboard-topbar in DOM (querySelector returns null); the admin workspace header is visible at the top with workspace pills + Light toggle + Notifications + RDX user pill + sign-out — ONE top bar only; notifications bell click opens the notification popup; KPI strip renders 5 tiles within available width WITHOUT being squeezed by the AI rail; AI rail is 320px wide on the right. (b) http://localhost:3086/admin DARK MODE — toggle to dark mode using the Light/Dark toggle in the admin header, verify: dashboard cards (.dashboard-chart, .dashboard-activity, .dashboard-actions, .dashboard-properties-table) have dark surface colors (NOT light); KPI tile labels use light-on-dark ink; AI rail background is dark; status pills use the dark variants of --color-status-* tokens. (c) MOBILE 390x844 light mode — admin dashboard stacks vertically with AdminWorkspaceHeader rendering correctly on mobile. (d) REGRESSION CHECKS — /admin/settings/profiles loads 200 no boundary; /owner sign-out icon 16px; /admin notification bell still gold gradient pill on light, appropriate dark on dark. (e) Save screenshots desktop light, desktop dark, mobile light to /tmp/kapital-next-v89-3-hf-20260605-screenshots/. Create branch codex/v89-3-hf-20260605, commit, push branch-only. Report: Branch SHA + base SHA; Files changed (admin-dashboard-screen.tsx, admin.css, tests/v89-3-dashboard.test.ts, manifest, audit doc); CDP evidence ONE top chrome only (no .dashboard-topbar in DOM); CDP evidence dark mode applies to dashboard cards (computed background colors are dark, not light); CDP evidence AI rail is 320px and KPI strip is not clipped; HF-2/3/V85-E regression checks confirm preserved; Verification ladder timings; All 7 CSS contract guards PASSING; Final manifest counts (still 75/62). Halt criteria: Budget violation; Any existing CSS contract guard breaks; check:v89-3-dashboard fails (would mean a needed V89-3 class was accidentally removed); Build doesn't produce 158/158; HF-2/HF-3/V85-E regress; AdminWorkspaceHeader does NOT render on /admin (would mean layout.tsx was accidentally touched); Dark mode still shows light cards. CONSTRAINTS: V89-3-HF fixes V89-3 only. Do NOT touch other admin pages, owner portal, resident portal, or marketing. Do NOT modify AdminWorkspaceHeader (it already works). Do NOT add new components. Do NOT add new guards or tests beyond the test assertion update. The fix is REMOVE the duplicate top bar + ADD dark mode coverage + FIX the grid layout. Document changes in the existing v89-3-dashboard-audit.md (extend it, don't create a new doc).
