---
id: attachment-you-are-firing-v89-3-hf3-v89-3-cleanup-hotfix-3-on-the-kapital-next-04a205d5
title: "You are firing V89-3-HF3 (V89-3 cleanup hotfix #3) on the kapital-next..."
source: attachment
createdAt: 2026-07-03T00:00:00.000Z
---

You are firing V89-3-HF3 (V89-3 cleanup hotfix #3) on the kapital-next repo. V89-3 and V89-3-HF2 introduced four lingering issues plus removed a real pre-existing capability. Fix all five in this cycle: (1) workspace selector pill in the top bar does nothing on click — wire it or remove it; (2) Ask kapital rail labeled "Coming in V90" but kapital-next ALREADY has a working assistant system at src/lib/actions/assistant.ts (sendTenantAssistantMessageAction + admin equivalent), src/lib/assistant-experience.ts (submitAssistantPrompt), src/app/admin/assistant/page.tsx, src/app/assistant-panel/admin/page.tsx — Ask kapital must connect to the existing system, not be a fake placeholder; (3) activity feed timestamps "May 17, 9:12 PM" wrap so "PM" lands on a second line on desktop — needs white-space: nowrap; (4) dark mode still leaves mid-body cards (.dashboard-chart, .dashboard-activity, .dashboard-actions) and properties table with LIGHT card backgrounds on dark background — V89-3-HF2 covered some classes but missed these; (5) the pre-V89-3 dashboard had user-customizable widget layouts (drag to reorder/show/hide KPI tiles + panels, saved per-user via existing src/lib/admin-productivity.ts AdminDashboardLayout type + src/lib/actions/admin-productivity.ts server action). V89-3 stripped this entirely. The V89 design reference Section 4.1 explicitly said to treat customization as "a later/secondary affordance rather than the primary dashboard anatomy" — meaning preserve it as a secondary mode, NOT remove it. Restore customization as a discreet secondary mode on top of the V89-3 cockpit. Branch-only. Do NOT fire merge from this chat. Use a fresh sibling clone. Standing rules in AGENTS.md apply. Strategic-debt budgets must hold: src/lib/notifications.ts 1473/1490, prisma/schema (combined) 5938/5938. All existing CSS contract guards must continue passing: check:ui-class-definitions, check:z-index-scale, check:hf-2-cold-load-locality, check:v85-task-e, check:v89-1-typography, check:v89-2-token-spine, check:v89-3-dashboard, check:v89-3-hf2-wireup. Inputs: Base is the V89-3-HF2 branch tip f0321771b3d1af9eb0f5505ccf5b2b373df5dc14 (V89-3-HF2 is not yet merged to main; V89-3-HF3 builds on it). Closes V89-3-HF3. Expected post-branch manifest UNCHANGED at 76/63 (the existing check:v89-3-hf2-wireup guard expands its assertions, no new guard). PRESERVE-EXISTING-FUNCTIONALITY AUDIT (do this BEFORE coding any of the 5 fixes): grep the codebase for any other pre-V89-3 dashboard functionality that V89-3 may have silently dropped. Specifically check for: hooks/effects on the original AdminDashboardScreen that aren't in the new one (e.g., websocket subscriptions, polling intervals, focus management, keyboard shortcuts), any imports that were in the V89-3 base SHA's admin-dashboard-screen.tsx but not in the current f0321771b3d1af9eb0f5505ccf5b2b373df5dc14 tip, any Prisma queries from src/lib/actions/admin-productivity.ts that are no longer being called. Document anything else found in the audit doc and fix it in this same branch — do NOT skip preserve-existing functionality issues. V89-3-HF3 scope (do exactly these): (1) AUDIT and either WIRE or REMOVE the "WORKSPACE Home" selector pill in AdminWorkspaceHeader. Investigate what it's semantically supposed to do (search for "workspace" in admin-workspace-header.tsx, admin-productivity.ts, admin-scope.ts). If it's a workspace/company selector, wire it to the existing scope handler (same one the "All companies" scope-filter-row uses). If it's redundant with the scope-filter-row, REMOVE it and keep only the breadcrumb. Do NOT leave it as non-functional click target. Document the choice in the audit doc. (2) WIRE Ask kapital rail to the existing assistant system. Investigate src/lib/actions/assistant.ts for the admin server action (find it — likely sendAdminAssistantMessageAction or equivalent). If only a tenant action exists, add an admin equivalent that uses the same submitAssistantPrompt from src/lib/assistant-experience.ts. Replace the V89-3-HF2 disabled Preview UI: input becomes ENABLED with real placeholder like "Ask about properties, tenants, leases…"; submit triggers the existing admin assistant action and either redirects to /admin/assistant or expands inline using the existing rendering; the three suggested prompt buttons pre-fill and submit OR link to /admin/assistant?prompt=<text>; remove all "Preview" labels and "Coming in V90" text. Sparkle icon stays. Collapse + localStorage persistence from V89-3-HF2 stays. DO NOT add new LLM integration; reuse existing kapital-next AI. (3) FIX activity timestamp PM line-wrap. In admin.css for .dashboard-activity .activity-row timestamp class (find exact class name), add `white-space: nowrap;` for desktop viewport (>= 768px or appropriate breakpoint). Mobile may wrap. CDP-verify computed white-space is "nowrap" on desktop. (4) COMPLETE dark mode coverage for missed classes. Add scoped rules under .app-theme-scope[data-app-theme="dark"] for: .dashboard-chart and its inner heading; .dashboard-activity and .activity-row; .dashboard-actions and .action-row; .dashboard-properties-table including thead, tbody tr, tbody tr:hover, .properties-table-row-thumbnail. Each consumes V89-2 dark tokens (--color-surface-base / --color-surface-raised for backgrounds, --color-border-subtle / --color-border-strong for borders, --color-ink-* for text). CDP-verify by toggling dark mode that computed background-color on .dashboard-chart, .dashboard-activity, .dashboard-actions, .dashboard-properties-table all resolve to dark surface (NOT white/#FFFFFF). (5) RESTORE dashboard customization as secondary mode. The existing src/lib/admin-productivity.ts has AdminDashboardLayout type, AdminDashboardKpiWidgetId, AdminDashboardPanelWidgetId. The existing src/lib/actions/admin-productivity.ts has the persist action. V89-3 stripped the consumer of these but the lib + action still exist. Add to the new V89-3 dashboard: a discreet "Customize" button in the page header right cluster (next to the date range pill set) using a small icon (Settings or Sliders from lucide-react). Clicking enters edit mode (state: isCustomizing). In edit mode: each KPI tile becomes draggable (use existing drag mechanism if there was one — check git history of admin-dashboard-screen.tsx via git log --all -p), each mid-body panel (chart/activity/actions) becomes draggable, the user can reorder via drag, hide tiles via a small X on each, show hidden tiles via an "Add widget" affordance. Save button commits the layout via the existing admin-productivity server action; Cancel discards. Saved layouts persist per-user. When the user returns to /admin, render the V89-3 cockpit using their saved AdminDashboardLayout — fall back to the default cockpit layout if no saved layout exists. The default cockpit layout (KPI strip first, chart/activity/actions triad, properties table, AI rail) MUST be what new users see. Customization preserves the V89 visual identity — tiles still use the V89 token system, MoneyAmount, mono data typography, etc. The "Customize" affordance must be unobtrusive when the user is not customizing (small icon button, not a giant pill). (6) UPDATE tests/v89-3-dashboard.test.ts to assert: Ask kapital input is NOT aria-disabled and HAS the assistant action wired (find a way to assert — e.g., the form has action attribute pointing to assistant, or a useFormState binding to the assistant action); workspace pill either has onClick wired OR does not exist in DOM; activity timestamp has white-space nowrap; "Customize" button is present in the page header; clicking Customize sets isCustomizing state; the dashboard layout consumer reads from the admin-productivity layout type. UPDATE the check:v89-3-hf2-wireup guard to assert these. (7) Manifest counts UNCHANGED at 76/63. Add a V89-3-HF3 manifest row: "V89-3-HF3: wired workspace selector or removed; Ask kapital connected to existing assistant; timestamp nowrap; dark mode mid-body + table coverage; customization restored as secondary mode." (8) Update docs/handoffs/v89-3-dashboard-audit.md noting: my (Claude's) V89-3 + V89-3-HF + V89-3-HF2 prompts repeatedly under-emphasized "preserve existing functionality" — this was the third instance (duplicate topbar, AI integration, dashboard customization). V89-3-HF3 restores the customization the V89 design reference explicitly said to keep as "later/secondary affordance." Document the PRESERVE-EXISTING-FUNCTIONALITY AUDIT as a required step for all future V89-N prompts. Verification ladder per AGENTS.md: npm ci; npm run architecture:proof (budgets hold); npm run local:smoke; npm run check:v89-3-hf2-wireup (updated assertions — PASS); npm run check:v89-3-dashboard; all CSS contract guards; npx vitest run --maxWorkers=8; npm run build (158/158). COLD-LOAD VERIFICATION — Playwright channel:"chrome", headless:false, fresh persistent profile per URL: PORT=3091 npm run dev. Cold-load with NO prior navigation: (a) http://localhost:3091/admin LIGHT — CDP verify workspace pill wired-or-removed; Ask kapital NOT aria-disabled with real placeholder; activity timestamp white-space:nowrap on desktop; Customize button present in page header (top right, near date range), clicking it sets isCustomizing state, KPI tiles become draggable in edit mode, Save commits via existing action; (b) http://localhost:3091/admin DARK — toggle dark mode; CDP verify computed background on .dashboard-chart, .dashboard-activity, .dashboard-actions, .dashboard-properties-table all DARK; (c) Test customization flow: enter customize mode, hide one KPI tile, save, reload page — verify hidden tile is still hidden (persistence works via existing action); (d) REGRESSION CHECKS — Cmd-K palette still works; /admin/settings/profiles 200 no boundary; /owner sign-out 16px; HF-3 notification bell still functional; HF-2 React-key fix preserved; (e) Save screenshots desktop light, desktop dark, customize mode, ai-rail-active, to /tmp/kapital-next-v89-3-hf3-20260605-screenshots/. Create branch codex/v89-3-hf3-20260605, commit, push branch-only. Report: Branch SHA + base SHA (V89-3-HF2 tip f0321771); Files changed including any pre-V89-3 functionality restored beyond the 5 explicit fixes; CDP evidence: workspace pill wired-or-removed, Ask kapital connected to assistant action, timestamp nowrap, dark mode covers all 4 missed classes, Customize mode functional and persists; HF-2/3/V85-E/Cmd-K/V89-3-HF2 regression checks; Verification ladder timings; All CSS contract guards PASSING; Final manifest counts (76/63 unchanged). Halt criteria: Budget violation; Any existing CSS contract guard breaks; Workspace pill still non-functional; Ask kapital still labeled "Coming in V90" or disabled; Timestamp wraps on desktop; Dark mode still light cards on dark; Customize button missing or doesn't persist; Any pre-V89-3 functionality found in the audit not restored; HF-2/3/V85-E/Cmd-K regress; Build doesn't produce 158/158. CONSTRAINTS: V89-3-HF3 reuses ALL existing kapital-next systems — the assistant integration, the admin-productivity layout persistence, the scope handlers. DO NOT add new LLM integration, new database tables, or new persistence systems. The customization restoration is a CONSUMER of the already-existing admin-productivity types and action — find them via git log on admin-dashboard-screen.tsx and re-instate the consumer. Document the architectural correction.
